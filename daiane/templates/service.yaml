apiVersion: v1
kind: Service
metadata:
  name: {{ include "daiane.fullname" . }}
  labels:
    {{- include "daiane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daiane.selectorLabels" . | nindent 4 }}
